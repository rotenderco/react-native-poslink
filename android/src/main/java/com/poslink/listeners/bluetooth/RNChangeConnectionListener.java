package com.poslink.listeners.bluetooth;

import com.facebook.react.bridge.ReactApplicationContext;
import com.poslink.listeners.RNListener;

public class RNChangeConnectionListener extends RNListener {
  public static final String NAME = "RNChangeConnectionListener";

  public RNChangeConnectionListener(ReactApplicationContext reactContext) {
    super(reactContext);
  }
}
